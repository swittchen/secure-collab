spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/securecollab
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  upload-dir: uploads
