spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/securecollab
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379

springdoc:
  swagger-ui:
    path: /swagger-ui.html

app:
  upload-dir: uploads

security:
  jwt:
    secret: d2c39483f1e4afc583db2198c9a3f27d1a2e594bf31e9c1cb582e60f87123456
    access-expiration-ms: 3600000
    refresh-expiration-ms: 604800000

